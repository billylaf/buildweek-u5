import { FC } from 'react';
import { type InputProps } from 'react-login-page';
export interface UsernameProps extends InputProps {
    label?: React.ReactNode;
    panel?: 'login' | 'signup';
}
export declare const Username: FC<UsernameProps>;
