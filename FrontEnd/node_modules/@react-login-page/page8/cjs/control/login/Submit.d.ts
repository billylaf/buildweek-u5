import { type FC } from 'react';
import { type ButtonProps } from 'react-login-page';
export interface Submit extends ButtonProps {
    panel?: 'login' | 'signup';
}
export declare const Submit: FC<Submit>;
